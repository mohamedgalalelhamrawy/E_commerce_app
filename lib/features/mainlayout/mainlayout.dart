import 'package:flutter/material.dart';

class Mainlayout extends StatelessWidget {
  const Mainlayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Main lay out"),
    );
  }
}